<Window x:Class="NetBrowser.Shell.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Net-browser Shell" Height="420" Width="720">
  <Grid Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TextBlock Text="Net-browser WPF Shell (demo)" FontSize="16" FontWeight="Bold"/>

    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
      <TextBox x:Name="UrlBox" Width="440" Height="28" Text="https://example.com"/>
      <Button Content="Go" Width="80" Height="28" Margin="8,0,0,0" Click="Go_Click"/>
      <Button Content="Ping" Width="80" Height="28" Margin="8,0,0,0" Click="Ping_Click"/>
    </StackPanel>

    <TextBox Grid.Row="2" x:Name="LogBox" FontFamily="Consolas" FontSize="12"
             IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
  </Grid>
</Window>